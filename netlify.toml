[build]
  publish = "public"
  command = "gatsby info && gatsby build"
[build.environment]
  NODE_ENV = "production"
  NODE_VERSION = "10.15.3"
  YARN_VERSION = "1.15.2"
  YARN_FLAGS = "--no-ignore-optional"
